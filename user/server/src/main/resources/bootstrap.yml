spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
  application:
    name: user
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/